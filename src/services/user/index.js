const { createAndReturnUser } = require('./create&ReturnUser')
const { validateUserAndCreateToken } = require('./validateUser&CreateToken')

module.exports = {
  createAndReturnUser,
  validateUserAndCreateToken
}
